apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: nginx-test-nas
spec:
    accessModes:
        - ReadWriteOnce
    resources:
        requests:
            storage: 1Gi
    storageClassName: nas
    volumeName: nginx-static-pv
